{
  "name": "mfci-command-center",
  "version": "4.0.0",
  "private": true,
  "description": "MFCI Command Center phone-first business operations app",
  "scripts": {
    "serve": "firebase emulators:start --only hosting,database",
    "deploy": "firebase deploy --only hosting,database"
  },
  "devDependencies": {
    "firebase-tools": "^14.23.0"
  },
  "dependencies": {
    "firebase": "^12.16.0"
  }
}
